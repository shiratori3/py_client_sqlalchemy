query_uncheck_all:
    task_vaild: True
    task_name: query_uncheck_all  # optional, the name of task
    request_method: POST
    url_type: url  # the key to read, conf_dict['url'][url_type] of settings.yaml
    conf_name: query\query_uncheck_all.yaml  # the abs path is cwdPath.joinpath('\\conf\\post' + conf_name)
query_uncheck_pushed:
    task_vaild: True
    task_name: query_uncheck_pushed  # optional, the name of task
    request_method: POST
    url_type: url  # the key to read, conf_dict['url'][url_type] of settings.yaml
    conf_name: query\query_uncheck_pushed.yaml  # the abs path is cwdPath.joinpath('\\conf\\post' + conf_name)
